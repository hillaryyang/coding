#include <iostream>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>

using namespace std;

int main() {
unordered_set<int> S; 
unordered_multiset<int> K; //hash table, key value pair, can't store duplicates

unordered_map<int, int> c;

return 0;
}